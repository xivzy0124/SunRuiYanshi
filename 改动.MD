
数据改为真实点比如10752，不要像10000然后少500然后变成9500这样
数据过滤这一块，除剔除无效帧、设置置信度条件外，还包含空值处理、异常值处理等，且符合足底压力数据和三维姿态数据的实际过滤情况
在字段映射转换环节，需要统一双源异构数据的字段格式、规整参数、实现数据结构化、标准化，经过这个环节数量是否有变化？请帮我推敲
字段计算环节部分主要完成校正重心、关节夹角等核心参数误差，除了这些还包含哪些，标识一下
融合的规则：以统一毫秒级时间戳为关联主键，设置50ms时序匹配阈值，仅保留同步有效帧，看看如何在页面中体现，要高大上一点
接口发布和数据入库应该是并行的